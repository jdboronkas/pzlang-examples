require CountingAbstractModule

module extends CountingAbstractModule ; abstract modules must be extended

    forced override bool IsGoodCount() ; since this was 'forced' to 'override' by a parent interface
        return true
    end func
    
end module