;;;
    interface keyword marks the module as an interface
    
    an interface module can not be constructed, can not have a constructor, and may only contain 'forced' functions (which are 'public')
;;;

interface module
    forced int Add (int a, int b)
    forced int Subtract (int a, int b)
end module